# 交接文档

> 项目：desktop-task-manager（桌面待办应用 + Android 桌面小部件）
> 技术栈：Tauri v2（Rust 后端 + WebView 前端）+ React + TypeScript + Vite + SQLite（tauri-plugin-sql）+ Android 原生小部件（Kotlin/RemoteViews）
> 分支：`v2.1-rework` ｜ 交接日期：2026-08-27

---

## 1. 项目概览

| 项 | 内容 |
| --- | --- |
| 桌面端 | Windows 桌面应用（系统托盘 / 全局快捷键 Ctrl+Shift+Space / 开机自启动 / WorkerW 桌面级挂载） |
| 前端 | `src/`（TypeScript + React），UI 组件、仓储层、业务服务层、Hooks、WebDAV 同步引擎 |
| 后端 | `src-tauri/src/`（Rust）—— 应用入口、系统集成、WebDAV HTTP 直连（reqwest）、平台条件化编译 |
| 移动端 | `src-tauri/gen/android/`（Tauri 生成的 Android 壳 + 手写小部件：任务列表、新建任务） |
| 数据 | 本地 SQLite `data.db`，前端负责建表/迁移/读写；小部件跨进程只读/写同一库 |
| 同步 | WebDAV（坚果云）双向合并同步，gzip 压缩，Last-Write-Wins |

### 目录结构

```
src/                    前端（WebView 内运行）
  components/           React 组件
  data/                 仓储层 / sync 引擎 / database.ts / types.ts / platform.ts
  hooks/                useTaskData.ts
  services/             业务服务（Task/Folder/Settings/WindowState）
  App.tsx / main.tsx    入口
src-tauri/              Rust 后端 + Android 壳
  src/                  lib.rs（入口/系统集成）/ main.rs / webdav.rs
  gen/android/          Tauri 生成的 Android 工程（含手写小部件 Kotlin）
  icons/ icons  Cargo.toml  tauri.conf.json  capabilities/
```

---

## 2. Git 状态

- 当前分支：`v2.1-rework`，本地已领先 `origin/v2.1-rework` 若干提交（用户当前无远程推送需求，按"仅本地提交"策略积累）。
- 最近提交：见 `git log --oneline -10`。
- **仓库提交约定（2026-09-05 起）**：
  - `scripts/` 目录下的构建/归档脚本**必须入仓**（`npm run release` 引用），本次补 commit `scripts/copy-release.mjs`。
  - `demo.html` / `src/demoMain.tsx` 是月视图本地预览辅助（绕过 Tauri 后端用样本数据驱动真实 `calendarView.tsx` 源码），**不入仓**——通过 `git add` 时显式跳过，不进 `.gitignore` 避免污染；保留工作区文件方便后续调样式。
  - `.workbuddy/` 为项目 memory 目录（含每日日志 `YYYY-MM-DD.md`、长期 `MEMORY.md`、技能缓存），不入仓。

---

## 3. 构建与运行环境

> 以下经验来自本次会话，供交接避免踩坑。

### 3.1 桌面端构建

```bash
npm install
npm run dev          # vite dev server（前端）
npm run tauri dev    # Tauri 桌面调试
npm run build        # 前端产物（vite build）
```

### 3.2 Android APK 构建

- **`cargo` 不在 PATH**：构建前先注入本仓库自带的 cargo bin 到 PATH（导致多次 `cargo not found`）。

```powershell
$env:Path = "D:\Claude code\TODO2\.cargo\bin;$env:Path"
cd src-tauri
.\gen\android\gradlew.bat -p .\gen\android assembleArm64Release
```

- 产物位于 `src-tauri\gen\android\app\build\outputs\apk\arm64\release\app-arm64-release.apk`。
- 发行安装包统一放置到 `D:\Claude code\TODO2\release\`，文件名 `desktop-task-manager-arm64-release.apk`。
- **TRAE Sandbox 限制**：Gradle 写临时 socket 文件可能被沙箱拦截报错，但**不影响 APK 产物**，产物仍正常生成。

### 3.2.1 `release/` 产物规范（重要，自 2026-09-05 起）
- **`release/` 只维护两个文件**：`desktop-task-manager-arm64-release.apk`（真机包）+ `DesktopTaskManager-v2.1.0-setup.exe`（Windows NSIS 安装器）。
- **不再保留**：`desktop-task-manager-arm64-debug.apk`、`*.msi`（MSI）、历史版本 `setup.exe`。发布时删除旧 debug.apk/msi/旧 exe，只保留当前两个。
- 桌面打包：`src-tauri/tauri.conf.json` 的 `bundle.targets` 已改为 **`"nsis"`**（仅出 NSIS，`tauri build` 不再产出 MSI）。若需 MSI 改回 `"all"`/`["nsis","msi"]`。
- 打包命令：`npm run release`（= `tauri build` + 自动复制安装包 + 便携版 exe 到 `release/`，文件名带版本号；脚本 `scripts/copy-release.mjs` 已加 try-catch 容错，app.exe 被运行中进程锁定时不中断）。
- **重复任务 Bug 已修复（2026-09-05）**：根因为同步快照遗漏重复任务字段导致 `repeatSeriesId` 被清空，撤销完成时系列清理失效。详见第 10 节。

### 3.2.2 `scripts/copy-release.mjs` 归档修复（2026-09-05）

`nsis` 目录里可能残留多个历史版本安装包（如 `1.0.0/2.0.0-setup.exe`），原先用 `readdirSync().find()` 按 **数组顺序**取第一个，会拿到旧版覆盖新构建。已改为按 **文件修改时间**取最新：

```js
const setups = readdirSync(nsisDir)
  .filter((f) => f.endsWith('-setup.exe'))
  .map((f) => join(nsisDir, f))
  .sort((a, b) => statSync(b).mtimeMs - statSync(a).mtimeMs);
if (setups.length > 0) installer = setups[0];
```

**当前 `release/` 包含**：arm64 APK + v2.1.0 安装包（NSIS）+ v2.1.0 便携版（命名 `DesktopTaskManager-v2.1.0.exe`，**便携版历史上未入仓**，仅作本地参考产物；若需入仓单独 add 即可）。

### 3.3 Rust 平台条件化（重要约定）

`src-tauri/src/lib.rs` 与 `Cargo.toml` 对桌面/Android 做了严格条件化：

- 系统托盘、全局快捷键、开机自启动等 **crate 顶部带 `#![cfg(not(any(target_os = "android", target_os = "ios")))]`**，Android 上根本不编译，因此 import 与注册均需 `#[cfg(not(target_os = "android"))]`，否则 Android 交叉编译报"找不到 crate"。
- `reqwest` 在 Android 用 rustls、桌面用 schannel/openssl（见 `Cargo.toml` 的 `[target.'cfg(target_os = "android")'.dependencies]`），避免 openssl-sys 交叉编译失败。

---

## 4. 最近一次迭代内容（v2.1-rework 小部件）

本轮针对小部件一次性修复了 4 个 bug 并重出 APK：

1. **重要图标字体消失**：颜色值 `0xFF9500` 缺 alpha 前缀 → `0xFFFF9500`（Android 需 8 位 ARGB，6 位 alpha=0x00=全透明）。
2. **自动解析时间未上线**：移植 `NaturalDateParser`（新增 `NaturalDateParser.kt`），并在 `WidgetAddTaskActivity` 接入 `updateParsePreview()` 实时解析预览。
3. **日历显示不一致**：改为与快速新建面板一致的 `renderPanel()` 动态快捷日期面板（替代原生 `DatePickerDialog`）。
4. **无法完成/撤销任务**（见第 5 章，**仍未解决**）：`TaskWidgetFactory` 改 `setOnClickFillInIntent`、`TaskWidgetProvider` 补 `setPendingIntentTemplate`。

修复时曾遇到的编译错误（已解决）：

- `WidgetAddTaskActivity.kt` 第 253/260 行 **"Val cannot be reassigned"**：`text = text` 参数遮蔽了 TextView 的 `text` 属性。改为 `this.text = subText / text`。

### 验证

- 构建成功（clean build 通过）。
- 前端 `npx tsc --noEmit` 通过；`npm test`（vitest）26/26 通过。

---

## 5. 已修复（代码级，待真机验证）—— 小部件无法完成 / 撤销任务

> **交接后修复记录（2026-08-27）**：现象为"点了完全没反应、列表不动"，根因定位为**点击分发 fill-in 附加信息在部分桌面丢失**——fill-in 原先设在列表项**子视图**（`item_check` 可点击、`item_title` 不可点击），而非官方集合小部件规范要求的**列表项根视图**，且模板 PendingIntent 缺少每实例唯一 data URI；导致广播收到 `extra_task_id == null` 后在 `handleToggleComplete` 静默返回。
>
> 改动：
> 1. `task_widget_item.xml`：根 `LinearLayout` 增加 `@+id/widget_item_root`；移除 `item_check` 的 `clickable/focusable`，让整行成为单一点击目标。
> 2. `TaskWidgetFactory.kt`：`setOnClickFillInIntent` 改设在 `widget_item_root`（官方规范）；`hasStableIds()` 改回 `false`。
> 3. `TaskWidgetProvider.kt`：模板 PendingIntent 增加 `EXTRA_APPWIDGET_ID` 与 `data = serviceIntent.data`（每实例唯一）；`handleToggleComplete` 改用 `db.update()` 记录受影响行数并补日志（tag `TaskWidget`）。
> 4. `WidgetDb.kt`：`openReadOnly/openWritable` 失败分支补 `Log.e`，新增 `resolvePath()` 供日志定位 DB 路径。
>
> 构建验证：`npx tsc --noEmit` 通过；`gradlew assembleArm64Release` **BUILD SUCCESSFUL**；APK 已更新至 `release/desktop-task-manager-arm64-release.apk`。
>
> **第 2 轮（真机复测后）**：设备上点击有**灰色选中动画但勾选框无反应**——说明 ListView 项点击已触发，问题聚焦在「广播处理」或「刷新」。本轮改动：
> 1. `TaskWidgetProvider.handleToggleComplete` 各分支加 **Toast 真机诊断**（无需 logcat）：`未收到任务ID（fill-in 丢失）` / `无法打开数据库` / `未匹配到任务（rows=0）` / `写库异常 …` / `已完成 ✓`、`已撤销 ✓`。
> 2. `onReceive` 增加日志：任何广播到达都打印 `action` 与 `taskId`（tag `TaskWidget`）。
> 3. `refreshWidget` 改为官方集合刷新写法：**仅 `notifyAppWidgetViewDataChanged`**，不再整卡 `updateAppWidget` 重绘（整卡重绘会重置 ListView 适配器，notify 可能被新绑定吃掉 → "写库成功但列表不更新"）。
>
> **真机验证（第 2 轮）**：**卸载 → 重装** `release/desktop-task-manager-arm64-release.apk` → 删除旧 widget → 重新添加 → 点击任务，按 Toast 区分：
> - **无 Toast** → 广播未送达（桌面 fill-in/模板未触发，需换机制）；
> - Toast `未收到任务ID` → fill-in 附加信息丢失；
> - Toast `无法打开数据库` / `未匹配到任务` / `写库异常` → 写库链路问题；
> - Toast `已完成 ✓` 但勾选框不变 → 刷新仍失败（继续查 notify/onDataSetChanged）；
> - Toast `已完成 ✓` 且勾选框变化 → 修复成功。
>
> 另可配合 `adb logcat -s TaskWidget` 交叉验证。
>
> **第 3 轮（诊断结果确认根因）**：真机 Toast 显示 `未收到任务ID（fill-in 丢失）`，设备为**华为鸿蒙 HarmonyOS 5.10.43**——证明**广播已送达（模板正常触发），但鸿蒙桌面丢弃了 fill-in 的每项附加信息**，fill-in 路径在鸿蒙上不可用。
> 本轮改动（`TaskWidgetFactory.getViewAt`）：
> 1. **每项独立 `setOnClickPendingIntent`**（优先路径）：任务 ID / 目标状态直接内嵌在每项自己的 PendingIntent 里（`requestCode = taskId.hashCode()` + 每项唯一 `widget-task://<id>` data URI 保证 PendingIntent 身份唯一），不依赖桌面合并，鸿蒙/原生桌面均生效。
> 2. 保留 `setOnClickFillInIntent` + `setPendingIntentTemplate` 作其它桌面（MIUI/EMUI 等）兜底；**每项 PendingIntent 后设置**，同一视图点击监听后者覆盖前者，确保优先走独立 PendingIntent。
> 3. 刷新维持第 2 轮的 `notify-only` 写法；Toast 诊断保留。
>
> **真机验证（第 3 轮）**：卸载 → 重装 → 删除旧 widget 重加 → 点击任务：
> - Toast `已完成 ✓ / 已撤销 ✓` 且勾选框变化 → 修复成功；
> - 仍 Toast `未收到任务ID` → 鸿蒙连每项独立 PendingIntent 也丢弃（极小概率），需改 Activity 中转方案；
> - 无 Toast → 每项独立 PendingIntent 点击未被桌面派发（灰动画仍在 = 项被按下但监听未触发）。
>
> **第 4 轮（第 3 轮复测仍"未收到任务ID" → 定位到自相矛盾）**：第 3 轮**同时**保留了模板（`setPendingIntentTemplate`）+ fill-in + 每项 PendingIntent，这本身就是错的——按 AOSP `OnClickHandler` 逻辑，**只要列表上有模板，点击就优先走「模板 + fill-in」路径并忽略每项独立 PendingIntent**；鸿蒙又丢 fill-in 附加信息，所以仍然拿不到任务 ID。
> 本轮改动：
> 1. `TaskWidgetProvider.updateWidget`：**彻底移除 `setPendingIntentTemplate`**（含模板 Intent），列表项点击完全交给每项独立 PendingIntent。
> 2. `TaskWidgetFactory.getViewAt`：**移除 `setOnClickFillInIntent`**，只保留每项独立 `setOnClickPendingIntent`（任务 ID 内嵌 + `widget-task://<id>` 唯一 data）。
> 3. Toast/日志补充 `intent.data`：可区分广播来源（`widget-task://…` = 每项 PendingIntent 路径；`intent:#Intent…` = 模板路径）。
>
> **真机验证（第 4 轮）**：卸载 → 重装 → 删旧 widget 重加 → 点击：
> - `已完成 ✓` 且勾选框变化 → 修复成功；
> - 无 Toast → 鸿蒙不派发集合项上的每项 PendingIntent（需改静态布局/Activity 中转）；
> - 仍 `未收到任务ID` → 查看 Toast 里 data：`widget-task://`（每项路径但 extras 被剥，几乎不可能）或 `intent:#Intent`（说明模板仍在生效，需再查）。
>
> **第 5 轮（第 4 轮复测：完全无 Toast → 定案改静态布局）**：鸿蒙桌面**既不派发集合视图项上的每项 PendingIntent，也不合并 fill-in 附加信息**——集合视图（ListView + RemoteViewsAdapter）的两种点击机制在鸿蒙全部失效。唯一所有桌面都可靠的是**普通视图上的 `setOnClickPendingIntent`**（刷新/新增按钮同机制）。
> 本轮改动：
> 1. `task_widget.xml`：删除 ListView，改为 **5 个固定任务行**（每行：复选框 ImageView + 标题 TextView + 截止/开始日期 TextView）。
> 2. `TaskWidgetProvider.updateWidget`：直接读库（`WidgetDb`）渲染**前 5 条任务**（未完成在前、按应用设置排序，排序逻辑自 `TaskWidgetFactory` 移植），每行挂**每项独立 `setOnClickPendingIntent`**（任务 ID 内嵌 + `widget-task://<id>` 唯一 data）。
> 3. `refreshWidget`：整卡重绘（静态布局无集合适配器，无需 `notifyAppWidgetViewDataChanged`）。
> 4. `TaskWidgetService` / `TaskWidgetFactory` **不再被使用**（代码与 Manifest 声明保留，未删除）。
>
> **真机验证（第 5 轮）**：卸载 → 重装 → 删旧 widget 重加 → 点击任意任务行：
> - Toast `已完成 ✓ / 已撤销 ✓` 且行内勾选图标变化 → 修复成功；
> - 仍无反应 → 说明该桌面连普通视图点击都异常（极不可能，刷新/新增按钮同机制）。
>
> **第 6 轮（第 5 轮复测：点击已生效 ✅，修复 4 个体验问题）**：
> 1. **小部件变更不同步到应用**（完成/撤销/新建）→ 应用内存态不感知外部改库：`useTaskData` 增加 `visibilitychange` 监听，**回到前台自动 `refresh()`**。
> 2. **刷新键无点击反馈** → 新增 `drawable/widget_tap_feedback.xml`（按下灰色高亮 selector，普通视图必支持），应用到刷新/新增按钮与任务行。
> 3. **已完成任务无删除线** → 根因：未完成 ≥5 时已完成任务排不进前 5 槽位、未渲染。改为槽位分配：**4 个未完成 + 第 5 槽固定显示「最近完成」**（删除线、灰色、可点撤销）；无已完成时第 5 槽回落第 5 个未完成。
> 4. **大半空白** → 5 行改为 `layout_height=0dp + layout_weight=1` **拉伸填满整个小部件高度**（行数不足时可见行等分高度）。
>
> **真机验证（第 6 轮）**：重装后确认：① 小部件完成/撤销/新建后打开 App 立即看到最新状态；② 刷新键按下有灰色高亮；③ 第 5 槽显示最近完成的任务（带删除线），点击可撤销；④ 小部件高度被任务行填满、无大片空白。
>
> **第 7 轮（第 6 轮全部验证通过 ✅ → 加滚动显示更多任务）**：用户要求滚动显示所有任务。集合视图（ListView）在鸿蒙已定案不可用，改用**普通 `ScrollView` 容器**（非集合视图、不走 RemoteViewsAdapter，行内每项 PendingIntent 与触摸滚动在所有桌面可靠）：
> 1. `task_widget.xml`：任务区改为 `ScrollView`（`scrollbars=vertical`），内嵌 **12 个静态任务行**（每行 minHeight 36dp + 按压反馈）。
> 2. `TaskWidgetProvider`：行数组扩至 12；槽位分配恢复**未完成在前、已完成在后，取前 12**（滚动区上限）。
> 3. **硬上限说明**：RemoteViews 不能动态增删行，小部件最多渲染 12 条；超出部分需在 App 内查看。若需更大上限，扩数组即可（行数/XML/ID 组同步扩）。
>
> **真机验证（第 7 轮）**：重装后确认：① 任务超过可视高度时列表可上下滑动；② 行内点击仍可完成/撤销；③ 已完成任务（删除线）在未完成任务之后按序出现。
>
> **第 8 轮（第 7 轮复测：小部件报"加载窗口小工具时出现问题" → 回退 ScrollView）**：ScrollView **不在 RemoteViews 官方支持视图清单**，鸿蒙渲染直接失败。**结论：小部件平台不支持任意内容滚动**（集合视图鸿蒙不可点、ScrollView 渲染失败），只能静态行 + 高度自适应。
> 本轮改动：
> 1. `task_widget.xml`：**删除 ScrollView**，恢复静态填充布局，行数 **5 → 6**（`layout_height=0dp + layout_weight=1` 等分填满高度；6 行在最小尺寸下每行 ≥18dp 图标高度，为无滚动下的安全上限）。
> 2. `TaskWidgetProvider`：行数组 12 → 6；显示顺序仍为**未完成在前、已完成在后，取前 6**。
>
> **真机验证（第 8 轮）**：重装后确认：① 恢复正常显示（无"问题加载"报错）；② 行内点击可完成/撤销；③ 高度被任务行填满。

**现象**（历史）：桌面小部件中的任务，点击复选框或标题，**无法切换"完成/撤销完成"状态**（列表无变化）。此前已按官方推荐写法改造过，但**实测仍无效**。

### 5.1 当前点击链路（代码已确认正确）

| 步骤 | 位置 | 说明 |
| --- | --- | --- |
| ① 每项设置填充意图 | `TaskWidgetFactory.kt:82-88` | 对 `item_check` / `item_title` 设 `setOnClickFillInIntent(fillIn)`，携带 `extra_task_id` 与 `extra_target_completed`（= `!item.completed`） |
| ② 列表设模板 | `TaskWidgetProvider.kt:98-104` | `setRemoteAdapter` 后设 `setPendingIntentTemplate(R.id.widget_list, PendingIntent.getBroadcast(..., ACTION_TOGGLE_COMPLETE))`，带 `FLAG_IMMUTABLE` 适配 Android 12+ |
| ③ 分发处理 | `TaskWidgetProvider.kt:27-35` | `onReceive` 命中 `ACTION_TOGGLE_COMPLETE` → `handleToggleComplete` |
| ④ 写库 | `TaskWidgetProvider.kt:38-57` | `WidgetDb.openWritable` → `UPDATE Task SET completed=?, completedAt=?, updatedAt=? WHERE id=? AND deleted=0` |
| ⑤ 刷新 | `TaskWidgetProvider.kt:69-79` | `requestRefresh` 逐个 widgetId 重绘 + `notifyAppWidgetViewDataChanged` 触发 `onDataSetChanged` 重载 |

**复杂度确认**：Manifest 已正确声明 `TaskWidgetProvider` 为 receiver、`TaskWidgetService` 带 `BIND_REMOTEVIEWS`，集合视图使用"模板 + 填充意图"方案，链路形式上符合 Android 官方推荐。**代码层面未见明显缺陷。**

### 5.2 可能根因（按优先级排查）

1. **设备安装的是旧 APK**（最常见）。上一轮构建成功并已放入 `release/`，但设备上可能未重装最新包。
   - 先重装 `release/desktop-task-manager-arm64-release.apk`，**删除旧 widget 后重新添加**，再测。

2. **可写库路径未命中**：`WidgetDb.resolveFile()` 依次尝试 `getDataDir()/desktop-task-manager/data.db`、`filesDir/desktop-task-manager/data.db`、`filesDir/data.db`。若 Rust `app_data_dir()` / 前端 `appDataDir()` 在 Android 的实际目录与这三者都不匹配，则 `openWritable` 返回 `null`，`handleToggleComplete` 在第 42 行 `?: return` **静默退出**（列表仍能显示说明只读路径命中，理论上读写应命中同一文件，需实测）。
   - 建议：在 `handleToggleComplete` 打印 `db.path`；在 `openWritable` 失败分支加 `Log.e`。

3. **同进程 SQLite 写锁**：widget 的 receiver/service 未设 `android:process`，与 WebView/主 Activity **同进程**；App 长时间运行持有连接（tauri-plugin-sql）可能持锁，写入触发 `SQLITE_BUSY`。已设 `busy_timeout=3000`，但异常被 `catch` 吞掉且无 UI 反馈。
   - 建议：`execSQL` 前后打日志，确认是否抛 `SQLITE_BUSY`。

4. **点击未送达**：部分桌面（MIUI/EMUI 等）对集合视图 fill-in 支持不一致，或模板 PendingIntent 与填充意图的 requestCode 冲突。
   - 建议：可临时改为每项独立 `setOnClickPendingIntent` 做对照试验（代价是某些桌面可能丢点击，用于定位是否分发问题）。

5. **主应用回写覆盖**：App 进程内 React 状态未感知小部件改动；若 App 随后有任何写库操作（`afterMutation`、自动同步、`notifyDataChanged`），可能把 `completed` 覆盖回原值，造成"看起来没变"。
   - 排查：观察是"点一下**完全没反应**"还是"**变了又弹回**"——这两种现象的根因不同。

### 5.3 建议排查步骤

1. 重装最新 APK + 重加 widget，排除安装产物旧。
2. 在 `handleToggleComplete` 加日志：打印 `db.path`、`execSQL` 前后、`openWritable` 是否 null。
3. 若 DB 写入正常但 UI 不变：确认 `notifyAppWidgetViewDataChanged` 是否触发 Service 的 `onDataSetChanged` 重新 `load()`。
4. 若怀疑分发：改用每项 `setOnClickPendingIntent` 对照。

---

## 6. 代码规范 & 冗余核查

- **变量名维护方案**：已建统一索引 [`VARIABLE_INDEX.md`](file:///d:/Claude%20code/TODO2/VARIABLE_INDEX.md)，覆盖类型/仓储/服务/工具/Hooks/Rust 命令/组件导出。
- **分层与单一职责**：`data`（状态/模型）→ `services`（业务变换）→ `hooks`（装配）→ `components`（UI）层次清晰；`TaskService` 委托 `TaskRepository`，`useTaskData` 用 `afterMutation()` 统一刷新 + 通知小部件。
- **注释**：解释"为什么"（如 `types.ts` 的 `deleted` 软删墓碑、`useTaskData` 忽略根级完成任务的注释）。
- **并发**：`Promise.all` 并行加载；`WidgetDb` 设 `busy_timeout` 应对跨进程/同进程竞争。
- **不适用项**：本项目为单机桌面应用，**微服务 / Redis / 消息队列**不适用。
- **已清理冗余**：删除 `webdavClient.ts` 中未使用的 `joinWebdavPath`（与 Rust `join_path` 重复），并同步更新 `index.ts` 导出与 `VARIABLE_INDEX.md`；`npx tsc --noEmit` 与 `npm test` 均通过。

---

## 7. 待办 / 技术债

1. ~~**小部件完成/撤销功能**（第 5 章）~~ —— ✅ 静态布局 + 回前台同步 + 按压反馈已全部真机验证通过；第 7 轮 ScrollView 方案在鸿蒙渲染失败（不在 RemoteViews 支持清单）已回退，现为 **6 行 weight 拉伸静态布局**（无滚动，安全上限 6）。APK 已重出放入 `release/`；**待第 8 轮真机验证**。
2. **`gen/android` 是生成物 + 手写混杂**：gradle/wrapper/mipmap 等为 Tauri 自动生成，`TaskWidgetProvider/Factory/Service/WidgetDb/WidgetAddTaskActivity/NaturalDateParser` 为手写。重跑 `tauri android` 重新生成可能**覆盖手写代码**，建议建立备份约定或将该目录手写部分与生成部分隔离说明。
3. **schema 双端各自维护**：`database.ts`（前端 `CREATE TABLE`）与 `WidgetDb.kt`（Kotlin 查询字段）需要保持一致，任何表结构变更须**同时更新两端**。
4. **测试覆盖有限**：目前仅 `src/data/sync/__tests__` 有 vitest 单测（merge/snapshot/throttle），仓储层与服务层尚无单元测试。

---

## 8. 重要约定速查

- **Android 颜色**：必须 8 位 ARGB（`0xFFRRGGBB`），6 位会导致 `alpha=0x00` 全透明。
- **小部件任务列表（重要，2026-08-27 真机定案）**：**不要用集合视图（ListView/GridView），也不要用 ScrollView**——华为鸿蒙 5.x 桌面不派发集合项点击、不合并 fill-in 附加信息，且 ScrollView 不在 RemoteViews 官方支持清单（渲染报"问题加载小工具"）。**小部件平台不支持任意内容滚动**。任务区为 **6 个 weight 拉伸的静态行**（普通视图，行内点击可靠），每行每项独立 `setOnClickPendingIntent`（任务 ID 内嵌，`requestCode=taskId.hashCode()` + `widget-task://<id>` 唯一 data）；安全上限 6 行（可扩，扩时同步 XML 与 Provider 的 ID 组，行数超过后最小高度下会过挤）。
- **小部件刷新**：不能用 `ACTION_APPWIDGET_UPDATE` 广播（不带 widgetId 时 AppWidgetProvider 不回调），需逐个 widgetId `notifyAppWidgetViewDataChanged`（不要整卡 `updateAppWidget` 重绘，会重置适配器导致 notify 丢失）。
- **数据库**：使用 `PRAGMA journal_mode = DELETE`（而非 WAL），否则小部件读不到未 checkpoint 的数据。
- **RemoteViewsService 绑定**：Intent 必须带 `content://` 数据 URI，否则部分桌面绑定失败导致列表空白。

---

## 9. App 端优化记录（2026-09）

1. **Android 端列表/设置无法手指滚动（根因修复）**：`folderTree.tsx` 任务行与文件夹标题**无条件设 `touchAction: 'none'`**，且 `startDrag` 在触屏 pointerdown 上 `preventDefault`——两者把触摸手势全拦下，Android 上手指在任何一行拖动都无法滚动。修复：
   - `startDrag` 增加 `if (e.pointerType !== 'mouse') return;` —— 触屏不启动拖拽排序（排序改为鼠标/桌面专属），让出触摸手势；
   - 移除任务行/文件夹标题的 `touchAction: 'none'`；
   - 主页面 `<main>` 与设置面板内容区滚动容器补 `touchAction: 'pan-y'` + `overscrollBehavior: 'contain'`。
2. **顶栏新增「新建文件夹」键**：`topBar.tsx` 在搜索框与「新建」之间加"新建文件夹"次按钮（FolderPlus 图标），`App.tsx` 接线到 `setDialog({type:'create-folder', parentId:null})`（与右键菜单"新建文件夹"同一对话框）。
3. **移动端视图切换改为顶部（同步键下方）分段控件**：`App.tsx` 的 `ViewTabs` 移动端样式从「底部固定导航条」改为「顶栏下方三格分段控件（列表/月/日，32px 触控高）」；移除底部固定导航与底部安全区占位（`<main>` 底部 padding 改 `calc(20px + env(safe-area-inset-bottom))`）。
4. **移动端不再照搬桌面右键菜单**：`contextMenu.tsx` 移动端过滤——任务/文件夹长按菜单仅保留有用项（编辑/添加子任务/完成/删除；文件夹的 新建任务(此文件夹)/新建子文件夹/重命名/删除），隐藏键盘快捷键提示；**全部展开/全部折叠/刷新/设置/退出 仅桌面显示**；`App.tsx` 根 `onContextMenu` 在移动端空白区长按直接 `return`（不弹桌面式菜单，新建/新建文件夹由顶栏承担）。
5. **坚果云服务器地址内嵌（V2.1.1）**：`types.ts` 新增 `DEFAULT_WEBDAV_URL = 'https://dav.jianguoyun.com/dav/'`；`database.ts` 建表默认值/初始数据用该常量，并新增迁移 `migrateSettingsFillDefaultWebdavUrl`（旧库 webdavUrl 为空自动回填）；`settingsPanel.tsx` 同步页移除"服务器地址"输入框，改为只读展示"坚果云（已内置）"，用户只需填账号与应用密码。
6. **移动端系统栏避让（导航栏不再遮挡内容）**：`MainActivity.kt` 在 `enableEdgeToEdge()` 后对内容根视图按 `WindowInsetsCompat`（systemBars + displayCutout）做原生内缩——比 CSS `env(safe-area-inset-*)` 可靠（部分 WebView/鸿蒙返回 0）；`App.tsx` 相应移除移动端对 `env(safe-area-*)` 的依赖（防双重留白），顶部/底部留固定视觉留白。
7. **设置新增「排障」页签**（参考《插件排障思路》：统一日志 + 设置内排障面板 + 状态自查 + 症状速查，适配本应用 WebDAV 同步）：
   - `sync/syncLog.ts`（新）：同步日志环形缓冲（400 条）+ localStorage 持久化（重启不丢），提供 `logSync/getSyncLogs/clearSyncLogs/exportSyncLogsText`，经 `sync/index.ts` 导出；
   - 打点：`scheduler.runSync`（自动同步：开始/跳过原因/成功/失败）、`App.handleOneClickSync`（一键更新）、`settingsPanel` 的 `handleProbe/handleOneClickSync`（连接测试/一键更新）；
   - `settingsPanel.tsx` 新增 `排障` 页签：状态自查（服务器内置地址/账号/应用密码/自动同步/上次同步）、操作（复制诊断信息→纯文本含摘要+日志 / 刷新 / 清空）、最近日志查看（按级别着色）、常见问题速查（连接 401→应用密码、自动同步未触发、慢/卡、双端冲突 LWW、上次基准）。
8. **同步功能参照《插件同步功能实现思路》优化（V2.1.1）**——对照文档结论：本应用已具备记录级 LWW 合并 + 删除墓碑（等价"三方收敛"）、gzip、流量节流跳过、远端目录自动创建、启动/30s 防抖/60min 定时触发、排障日志。本轮补齐文档强调的两块：
   - **瞬态失败退避重试**（对应"503 退避重试 / 恢复"维度）：`webdavClient.ts` 新增 `withRetry`，对网络抛错与 `429/500/502/503/504` 最多重试 3 次（退避 0.8s/2.4s），重试写入排障日志；**重试耗尽后抛错而非当作"远端不存在"**（防把限流误判成首次同步而覆盖远端）；
   - **同步互斥串行**（对应"并发控制 inFlight"）：`engine.ts` 新增 `enqueueSync` promise 链互斥，自动同步/顶栏一键更新/设置面板同步重叠时排队串行执行（写排障日志），避免并发下载-合并-PUT 竞争双写远端；另导出 `isSyncRunning()`。
   - 暂缓项（文档有、本应用场景价值低或成本高，后续可按需做）：每设备"仅上传/仅下载"策略、冲突 diff3/智能合并（记录库为 LWW 语义）、可配置同步间隔（当前 60min 为坚果云流量配额折中）。
9. **设置面板微调（同步已确认）**：
   - **排障页去图标**：移除状态自查表头 Bug、状态行 XCircle/CheckCircle2、操作按钮 Clipboard/RefreshCw/Trash2（未对齐），只留文本；相应精简 lucide 导入。
   - **同步页移除"服务器地址"栏**：坚果云地址已内置，直接删除该只读展示块（介绍文案与账密指南仍保留"已内置"说明），只留账号/应用密码输入。
   - **自动同步逻辑确认**：仍为「启动立即同步一次 + 数据变更防抖 30s + 每 60 分钟定时兜底 + 记录级 LWW 双向合并」（`scheduler.ts` DEBOUNCE_MS=30_000、INTERVAL_MS=60*60_000；`App.tsx` 启动 `configureAutoSync+startAutoSync`）。与设置页文案一致，无需改动。
10. **V2.1.2 一批（部分落地，大项待确认）**：
    - **移除上次同步时间显示**：`topBar.tsx` 删除第三行（上次同步时间/同步中），`App.tsx` 不再传 `lastSyncedAt/lastSyncAction`；设置-同步 状态区仅保留最近一次结果（`settingsPanel.tsx`）。同步时间仍记录在 Settings（`lastSyncedAt/lastSyncAction`），只是不再展示。
    - **默认同步策略**：`Settings.syncPolicy`（`twoWay`/`uploadOnly`/`downloadOnly`，默认 `twoWay`）——schema 建表默认+迁移 `migrateSettingsAddSyncPolicy`、`SettingsRepository` select/update、`engine.syncMerge/doSyncMerge/syncAuto` 按策略执行（uploadOnly→`uploadLocal`，downloadOnly→`downloadRemote`，twoWay→合并）；设置-同步 新增「默认同步策略」三段选择；自动同步与一键更新按该策略。
    - **顶栏功能键**（`topBar.tsx`，`App.runSyncWithPolicy`）：同步图标旁新增 上传云端 / 覆盖本地（强制策略立即执行）、已删除（打开已删除任务查看）。
    - **任务「已过期」标签**（`taskItem.tsx`）：超截止且未完成时标题旁显示浅紫底白字「已过期」（`isTaskExpired`/`ExpiredBadge`）。
    - **已删除任务**：`TaskRepository.getAllDeleted/purgeDeleted`、`TaskService.getAllDeleted/purgeDeleted`、`database.ts` 初始化时物理清除 deleted=1 且 updatedAt≤now-30d；`App` 顶栏「已删除」打开查看弹窗（删除线标题+删除时间，30 天内保留）。
    - **文案/外观**：「开启 Windows 通知」→「开启系统通知」；移除「默认提醒时间」选择（保留 reminderOffset 默认值，具体按任务提醒为大项后续）；移动端设置-外观 隐藏 毛玻璃效果/透明度（`isMobile` 包裹）。
    - **暂缓大项（需确认语义后做）**：#7 按任务提醒（新建/编辑选「提醒」后弹日期时间选择器、圈出截止日期、未设截止先提示）；#8 截止时间「重复」规则（每天/每周x/每月/每年/任意多天，含日历视图）；手机端小部件「外观」键（毛玻璃/透明度/主题）。
11. **（用户确认语义后）提醒 + 重复 + 小部件外观（V2.1.2）**：
    - **按任务提醒（真发系统/手机通知）**：Task 新增 `reminderAt`（提醒时刻）+ `reminderFired`（是否已触发）；`editTaskDialog.tsx`/`quickCapture.tsx` 增加「提醒」——需先设截止（未设截止显示红字提示），选 x月x日x时x分（datetime-local，默认等同截止，界面高亮显示截止时间）；`App.tsx` 移除旧的"默认提醒时间偏移"提醒逻辑，改为 30s 轮询 `TaskService.getDueReminders` → `sendNotification`（系统/手机通知）+ `markReminderFired`（防重复）。已删"默认提醒时间"设置项（`reminderOffset` 仅保留 DB 默认值）。
    - **重复规则（完成上一实例后自动生成下一实例并顺延截止）**：Task 新增 `repeatRule`（daily/weekly/monthly/yearly/custom）+ `repeatIntervalDays`（custom 每 N 天）；`TaskService.toggleTaskCompleted` 完成时若带重复规则与截止时间 → `spawnNextInstance` 生成下一实例（复制标题/备注/优先级/容器，截止按规则顺延：每天+1d、每周+7d、每月/每年按日钳制、自定义+Nd）；UI 提供「重复」下拉（无/每天/每周/每月/每年/自定义每 N 天）。
    - **小部件「外观」键**：`task_widget.xml` 头部新增"外观"按钮 → `WidgetAppearanceActivity.kt`（主题 浅色/深色、毛玻璃 开/关、透明度 SeekBar）存 `SharedPreferences("widget_appearance")`，`TaskWidgetProvider.readAppearance/applyAppearance` 应用于文字颜色（主题）与整体 alpha（透明度近似半透明），改动即时 `requestRefresh` 全部小部件；`strings.xml`/`AndroidManifest.xml` 相应登记。说明：RemoteViews 不支持真模糊，"毛玻璃"用近似半透明背景实现（用户已接受）。
    - 验证（x86_64 模拟器）：新建任务弹窗出现「提醒」「重复（每天/每周/每月/每年/自定义每 N 天）」控件；应用正常启动。提醒触发与递归生成（需真实数据/等待）及小部件桌面包级效果建议真机确认。
12. **（用户后续反馈）已删除/重复/小部件交互**：
    - **已删除任务**：`App.tsx` 的"已删除"弹窗改为按主视图展示任务信息（标题+截止日期/优先级、删除时间）并逐条提供**「取消删除」**（`restoreTask` + 实时从列表移除）；`Task` 顶层字段/迁移见上。
    - **重复任务**：新增 `repeatSeriesId`（重复链标识），`TaskService.spawnNextInstance` 继承之；`useTaskData` 加载时按系列统计"已重复次数"并挂到 `TaskWithSubtasks.repeatCount`，`taskItem` 显示「重复×N」徽章；`contextMenu` 增加**「结束重复」**（仅重复任务显示，App 清除 repeatRule）。
    - **小部件交互**：① 新增/外观 改为 `NEW_TASK | EXCLUDE_FROM_RECENTS`（去掉 CLEAR_TOP）作为独立任务从桌面打开，退出后**返回桌面**而非应用；② `WidgetAppearanceActivity` 重写为贴合应用风格的浅色底+圆角卡片+品牌蓝强调；③ 新增 **上/下翻页**：`task_widget.xml` 底部加 ▲/页码/▼，Provider 按 per-widgetId 偏移分页（`widget_scroll` prefs），`handleScroll` 调整偏移并 `requestRefresh`，`updateWidget` 按 `offset` 取 `SLOT_IDS.size` 条渲染。说明：小部件不支持真滚动，用"上/下分页"达成"点击上下键实现任务滚动"。
    - 验证：前端 `tsc` 通过；x86_64 包编译成功（含外观 Activity / 翻页 / 重复徽章）；模拟器确认应用正常启动、新建任务可创建、已删除弹窗结构正确。小部件桌面包级效果（返回桌面/翻页/外观风格）建议装机后真机确认。
13. **小部件外观修正（用户反馈）**：
    - **透明度不影响文字**：`applyAppearance` 改用 `setBackgroundColor`（仅背景基色带 alpha，毛玻璃开→按透明度半透明、关→不透明），**不再**用整卡 `setAlpha`（原先会让文字一起透明）。
    - **主题真正生效**：主题不仅用于标题/行/截止文字，还作用于**背景基色 + 全部文字**（浅色 #F9F9FB 底/深色 #1D1D1F 底；标题、刷新/外观、新增、上/下、页码、空态文字分别取深/浅/品牌色），`bindDeadlineText` 也按 dark 调整"无截止/开始"颜色。x86_64 编译通过；桌面包级效果请真机放置小部件→外观 调整确认。
14. **完成/重复标签细化（用户反馈，已完成 ✅ 2026-09-05）**：
    - **停止重复仍保留标签**：`RepeatBadge` 显示条件从 `repeatRule` 改为 **`repeatSeriesId`**（结束重复后 seriesId 仍在，标签保留、只是不再更新）；`TaskService.createTask` 对带重复规则的任务首实例即设 `repeatSeriesId=自身 id`（模板也计入累计次数）。
    - **文案**：「重复×N」→「**累计完成×N**」。
    - **位置**：`已过期` 与 `累计完成×N` 徽章从标题行移到**与截止时间同一行**（`DateBadge` 元数据行，即任务描述下方）。
    - **已完成聚类**：`CompletedSection` 重写——无重复系列任务平铺；**重复系列自动聚类**（聚类头显示标题+「累计完成×N」，点击展开显示各实例，实例可点击恢复）。
    - **标签样式对齐**：`ExpiredBadge`、`RepeatBadge` 与截止时间标签统一 `fontSize=11, padding='2px 8px', borderRadius=999, lineHeight=1.4`，同一行所有徽章高度一致。
    - **已完成区按完成时间排序**：`CompletedSection` 将非重复任务与系列聚类合并为一个列表，统一按 `completedAt` 降序排列（系列聚类取组内最新一条的 `completedAt`），"结束重复"刚完成的任务按时间自然穿插，不再沉底。
15. **月视图重写（V2.1.3，用户多次反馈迭代 ✅ 2026-09-05）**：
    - **月格药丸**：`DayCell` 不再只显示"圆点+任务数"，改为**直接列出当天任务**——每条任务是圆角药丸，含勾号 ✓ + 截断标题；**重要任务 → 实心蓝底白字（`#0a84ff`）**；普通任务 → **浅蓝底深蓝字（`#d4e6ff`/`#0b3e8f`）**；单格最多 3 条，超出显示 `+N`。
    - **行内展开面板**（用户关键反馈）：点击日期格不再固定在网格底部展开，而是**插入到该日期所在行与下一行之间**——点 9月5日（第1行）→ 面板在第1/2行之间；点 9月19日（第3行）→ 面板在第3/4行之间。渲染从"一次性铺 42 格"改为**逐周渲染 6 行**，选中行后立即插入详情面板，并把后续周向下推开；面板出现时自动 `scrollIntoView` 滚入可视区。
    - **详情面板展示已完成任务**：新增 `completedTasks` 入参；面板任务按"未完成在前（按截止早→晚）+ 已完成在后（按完成时间新→旧）"合并展示；已完成行用**灰色 + 删除线标题**，勾选圆钮显示为绿色实心（点撤销）；未完成行沿用列表样式——勾选圈与 `taskItem.tsx` 完全对齐（未完成=描边圆，普通灰/重要橙 `#ff6b3d`；完成=绿色 `--state-success` 实心 + 白勾；hover 蓝 `--primary` 描边）。
    - **「+ 添加事项」入口**：选中日期后底部出现按钮，调用 `onAddTask(timestamp)`，App 中打开 QuickCapture 并**预填该日**；App.tsx 接线 `setPrefillDate(ts)`。
    - **App 业务集成**：月视图勾选走 `onToggleTask(id)` → `handleToggleCompleted(id)`；撤销走真实 `restoreTask` 路径（不走 `toggleTaskCompleted`，与已完成区行为统一）；App.tsx 区分 toast 文案"已完成" vs "已恢复"。
    - **清理空壳 UI**：移除顶部工具条右上角的 `MoreHorizontal` 按钮（无 onClick 的死按钮）以及对应 `lucide-react` import 与注释。
    - **预览调试辅助**：本轮同时在 `src/demoMain.tsx` 与 `demo.html` 写了**本地预览壳**（用样本数据驱动真实 `CalendarView` 组件，绕过 Tauri 后端），`http://localhost:1420/demo.html` 即可访问；**这两个文件不入仓**（不入 `.gitignore`，仅 `git add` 时跳过），保留工作区便于调样式/验证交互。
    - **验证**：`tsc --noEmit` ✓；`vitest run src/data/sync` 26/26 ✓；`npm run release` ✓（2m52s 完成）；安装包哈希与构建源一致。

---

## 10. 重复任务"撤销完成产生重复实例"Bug —— 已修复（2026-09-05）

### 10.1 现象
1. 建一个**重复**任务（如「每天」，需设截止时间）。
2. 点击勾选完成 → 自动生成"下一实例"（正常）。
3. 去「已完成」区点击该任务（撤销完成）→ 出现**两条同样的重复任务**。
4. 反复 完成→撤销 可**越积越多（数据爆炸）**。

### 10.2 根因
- **表层**：撤销清理原来只删 `repeatNextId` 单链指向的那一条，且以 `repeatNextId` 是否非空作为触发条件。第一轮撤销时 `repeatNextId` 新鲜可用所以正常，第二轮若 `repeatNextId` 丢失则清理静默跳过 → 残留累积。
- **深层（关键）**：**同步快照遗漏重复任务字段**。`sync/snapshot.ts` 的 `buildSnapshot` 导出 Task 时完全没有包含 `repeatRule`、`repeatIntervalDays`、`repeatSeriesId`、`repeatNextId`、`reminderAt`、`reminderFired` 这 6 个字段；`importer.ts` 的 `INSERT OR REPLACE` 也不写这些列。每次同步后这些字段被重置为 NULL → `repeatSeriesId` 变成 NULL → `cleanupUndoSeries` 里 `if (!task.repeatSeriesId) return;` 直接跳过，不清理未完成实例 → 重复任务累积。

### 10.3 修复内容
1. **同步快照补全字段**（根因修复）：
   - `sync/types.ts`：`SyncTask` 补全 6 个字段；`SYNC_SCHEMA_VERSION` 从 2 升到 **3**。
   - `sync/snapshot.ts`：`buildSnapshot` 导出新字段；`parseSnapshot` 兼容 v1/v2 旧快照（缺字段补 null/false）。
   - `sync/importer.ts`：`importSnapshot` 和 `importMerged` 的 INSERT 语句写入新字段。
   - 测试：`snapshot.test.ts`/`merge.test.ts`/`throttle.test.ts` 的 `makeTask` 补全新字段，26 个同步测试全部通过。
2. **撤销清理改为按整系列清理**：
   - `TaskRepository.deleteIncompleteSeries(seriesId, exceptId)`：一条 SQL 软删该系列所有未完成实例（排除被恢复任务本身）。
   - `TaskService.cleanupUndoSeries(task)`：主路径按 `repeatSeriesId` 清理整系列；回退路径 `repeatSeriesId` 缺失时按 `repeatNextId` 单链清理。
   - `restoreTask`（已完成区点击）和 `toggleTaskCompleted` 撤销分支（勾选撤销/Ctrl+Z）都改走 `cleanupUndoSeries`。
3. **存量脏数据修复**：`database.ts` 新增 `migrateTaskRepairRepeatSeriesId`，对 `repeatRule` 非空但 `repeatSeriesId` 为空的重复任务，把 `repeatSeriesId` 设为自身 id。
4. **"结束重复"原子操作**：`TaskService.stopRepeat(id)` 重写——清除重复规则 + 标记已完成（直接 `markCompleted`，绕过 spawn 逻辑）；`useTaskData.stopRepeat` 暴露；`App.tsx onStopRepeat` 直接调用。
5. **已完成区按 `completedAt` 混合排序**：见第 9 节第 14 项。
6. **标签样式对齐**：见第 9 节第 14 项。

### 10.4 验证
- `tsc --noEmit` ✓；`vitest run src/data/sync` 26 tests passed ✓；`tauri build` ✓。
- 安装包已归档 `release/DesktopTaskManager-v2.1.0-setup.exe`。
- 反复 完成→撤销 多轮不再累积；同步后 `repeatSeriesId` 不再被清空；结束重复后任务移入已完成区并按时间排序。

### 10.5 已知遗留
- **小部件完成路径（Kotlin）**：`TaskWidgetProvider.handleToggleComplete` 仍直写 DB，不经过 TS 业务逻辑，不生成下一实例、不写 `repeatNextId`。在小部件上完成的重复任务，App 里不会自动出现下一实例。建议后续统一两条路径。
- **Android APK**：`release/desktop-task-manager-arm64-release.apk` 为旧版，**尚未包含本次修复**，需重新 `gradlew assembleArm64Release` 构建。
